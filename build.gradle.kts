// Root project: a container for the :core, :relay and :cli modules.
// Plugin versions are declared here (apply false) and applied per-module.
plugins {
    kotlin("jvm") version "2.2.10" apply false
}
